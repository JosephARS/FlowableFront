import { CalcularVencimientoPipePipe } from './calcular-vencimiento-pipe.pipe';

describe('CalcularVencimientoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularVencimientoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
