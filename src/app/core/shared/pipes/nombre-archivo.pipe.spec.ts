import { NombreArchivoPipe } from './nombre-archivo.pipe';

describe('NombreArchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreArchivoPipe();
    expect(pipe).toBeTruthy();
  });
});
