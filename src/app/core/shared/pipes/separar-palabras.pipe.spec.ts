import { SepararPalabrasPipe } from './separar-palabras.pipe';

describe('SepararPalabrasPipe', () => {
  it('create an instance', () => {
    const pipe = new SepararPalabrasPipe();
    expect(pipe).toBeTruthy();
  });
});
